# LeeDiD 0.0.0.9000

* Initial development version.
* Core identification/estimation pipeline for the intensive margin effect
  under joint sample selection: `compute_ao_share()`, `empirical_cdfs()`,
  `cic_qtt()`, `cic_bounds()`, `bootstrap_se()`, `imbens_manski_ci()`.
* `leedid()` end-to-end estimator with `print()`/`summary()`/`plot()` methods.
* Simulated example dataset `leedid_sim`.
* Not yet submitted to CRAN.
