% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leedid.R
\name{leedid}
\alias{leedid}
\title{Estimate the intensive margin treatment effect under joint selection}
\usage{
leedid(
  data,
  yname,
  tname,
  idname,
  gname,
  selection_vars,
  t,
  tmin1,
  quantiles = seq(0.05, 0.95, 0.05),
  boot = TRUE,
  biters = 999,
  coverage = 0.95,
  seed = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame containing a two-period panel.}

\item{yname}{Name of the outcome column.}

\item{tname}{Name of the time column.}

\item{idname}{Name of the unit id column.}

\item{gname}{Name of the (time-invariant) group column, coded 0
(control) / 1 (treated).}

\item{selection_vars}{Character vector of at least two (0/1)
selection-indicator column names. A unit is "observed" at a period iff
all of these equal 1.}

\item{t}{Value of \code{tname} identifying the post-treatment period.}

\item{tmin1}{Value of \code{tname} identifying the pre-treatment period.}

\item{quantiles}{Numeric vector of quantiles in (0, 1) at which to
estimate the intensive margin QTT (bounds). Default
\code{seq(0.05, 0.95, 0.05)}.}

\item{boot}{Logical; compute bootstrap standard errors and confidence
intervals? Default \code{TRUE}.}

\item{biters}{Number of bootstrap replications, if \code{boot = TRUE}.}

\item{coverage}{Desired coverage probability for the Imbens-Manski
confidence intervals. Default \code{0.95}.}

\item{seed}{Optional integer seed for the bootstrap.}

\item{quiet}{Suppress bootstrap progress messages? Default \code{FALSE}.}
}
\value{
An object of class \code{"leedid"}: a list with the AO shares
(\code{pi_0}, \code{pi_1}), the unconditional CiC ATT and QTT, the AO att/QTT
bounds, the classical two-way fixed-effects DiD point/bound
comparison estimates, standard errors and confidence intervals (if
\code{boot = TRUE}), and the matched call.
}
\description{
End-to-end estimator for the average and quantile intensive-margin
treatment effect on the treated, partially identified among
"Always-Observed" units by adapting the Horowitz-Manski-Lee bounds to
the Changes-in-Changes framework, using multiple jointly required
sources of sample selection (Viviens, 2026).
}
\examples{
data(leedid_sim)
fit <- leedid(
  leedid_sim,
  yname = "y", tname = "time", idname = "id", gname = "group",
  selection_vars = c("employed", "responded"),
  t = 2, tmin1 = 1, boot = FALSE
)
fit
}
