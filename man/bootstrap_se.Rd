% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{bootstrap_se}
\alias{bootstrap_se}
\title{Bootstrap standard errors for the LeeDiD estimates}
\usage{
bootstrap_se(
  data,
  yname,
  tname,
  idname,
  gname,
  selection_vars,
  t,
  tmin1,
  quantiles,
  biters = 999,
  seed = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame containing a two-period panel.}

\item{yname}{Name of the outcome column.}

\item{tname}{Name of the time column.}

\item{idname}{Name of the unit id column.}

\item{gname}{Name of the (time-invariant) group column, coded 0
(control) / 1 (treated).}

\item{selection_vars}{Character vector of at least two (0/1)
selection-indicator column names. A unit is "observed" at a period iff
all of these equal 1.}

\item{t}{Value of \code{tname} identifying the post-treatment period.}

\item{tmin1}{Value of \code{tname} identifying the pre-treatment period.}

\item{quantiles}{Numeric vector of quantiles at which to bootstrap the
QTT bounds curve.}

\item{biters}{Number of bootstrap replications.}

\item{seed}{Optional integer seed, set once before resampling begins.}

\item{quiet}{If \code{FALSE} (default), prints one progress message every
~10\% of replications.}
}
\value{
A list with \code{se} (a one-row data frame of standard errors for
\code{att}, \code{att_lb}, \code{att_ub}) and \code{se_qtt} (a data frame with columns
\code{quantile}, \code{se_lb}, \code{se_ub}).
}
\description{
Resamples units (with replacement, keeping both periods of a unit
together) and recomputes the AO shares, the CiC point estimate, the AO
bounds, and (if \code{fixest} regressions are requested) the classical DiD
point/bound estimators, on each replicate. Standard errors are the
empirical standard deviations of the resampled statistics.
}
