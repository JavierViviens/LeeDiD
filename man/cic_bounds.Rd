% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds.R
\name{cic_bounds}
\alias{cic_bounds}
\title{Bounds on the intensive margin quantile treatment effect}
\usage{
cic_bounds(epc, quantiles, pi_0, pi_1)
}
\arguments{
\item{epc}{A list as returned by \code{\link[=empirical_cdfs]{empirical_cdfs()}}.}

\item{quantiles}{Numeric vector of quantiles in (0, 1) at which to
evaluate the effect.}

\item{pi_0, pi_1}{Estimated AO shares for the control and treated groups
(see \code{\link[=compute_ao_share]{compute_ao_share()}}).}
}
\value{
A data frame with columns \code{quantile}, \code{lb}, and \code{ub}.
}
\description{
Lower and upper bounds on the quantile treatment effect on the treated
among Always-Observed units, combining the Horowitz-Manski-Lee trimming
argument with the Changes-in-Changes counterfactual distribution.
}
\examples{
set.seed(1)
df <- data.frame(
  y = rnorm(200),
  time = rep(1:2, each = 100),
  group = rep(rep(0:1, each = 50), 2)
)
epc <- empirical_cdfs(df, yname = "y", tname = "time", gname = "group",
                      t = 2, tmin1 = 1)
cic_bounds(epc, quantiles = c(0.25, 0.5, 0.75), pi_0 = 0.9, pi_1 = 0.85)
}
