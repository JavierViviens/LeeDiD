% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection.R
\name{compute_ao_share}
\alias{compute_ao_share}
\title{Share of "Always Observed" units in each group}
\usage{
compute_ao_share(data, idname, tname, gname, selection_vars, t, tmin1)
}
\arguments{
\item{data}{A data frame containing a two-period panel.}

\item{idname}{Name of the unit id column.}

\item{tname}{Name of the time column.}

\item{gname}{Name of the (time-invariant) group column, coded 0
(control) / 1 (treated).}

\item{selection_vars}{Character vector of at least two (0/1)
selection-indicator column names. A unit is "observed" at a period iff
all of these equal 1.}

\item{t}{Value of \code{tname} identifying the post-treatment period.}

\item{tmin1}{Value of \code{tname} identifying the pre-treatment period.}
}
\value{
A list with components \code{pi_0}, \code{pi_1} (the estimated AO shares
for the control and treated groups) and \code{detail}, a data frame with
the per-source rates used to construct them.
}
\description{
Estimates the proportion of units in each group (\code{pi_0} for the control
group, \code{pi_1} for the treated group) that would be observed in the
estimation sample under \emph{either} treatment arm ("Always Observed", AO
units), following the Horowitz-Manski-Lee bounds approach adapted to
allow for \strong{multiple, jointly required sources of sample selection}
(e.g., employment status \emph{and} survey response).
}
\details{
A unit is treated as observed at a given period if and only if all of
\code{selection_vars} equal 1 in that period (their indicator product). For
each individual selection source, the share of units that would have
been selected under the counterfactual treatment arm is extrapolated
from the other group's observed-to-pre-period selection ratio, in the
same spirit as the Changes-in-Changes counterfactual construction. This
requires a strictly positive pre-period selection rate on every source,
in both groups, and at least one unit per group observed on all sources
in both periods.
}
\examples{
set.seed(1)
df <- data.frame(
  id = rep(1:200, each = 2),
  time = rep(1:2, 200),
  group = rep(rep(0:1, each = 2), 100),
  employed = rbinom(400, 1, 0.8),
  responded = rbinom(400, 1, 0.9)
)
compute_ao_share(df, idname = "id", tname = "time", gname = "group",
                  selection_vars = c("employed", "responded"),
                  t = 2, tmin1 = 1)
}
