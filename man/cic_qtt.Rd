% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds.R
\name{cic_qtt}
\alias{cic_qtt}
\title{Changes-in-Changes quantile treatment effect on the treated}
\usage{
cic_qtt(epc, quantiles)
}
\arguments{
\item{epc}{A list as returned by \code{\link[=empirical_cdfs]{empirical_cdfs()}}.}

\item{quantiles}{Numeric vector of quantiles in (0, 1) at which to
evaluate the effect.}
}
\value{
A numeric vector of the same length as \code{quantiles}.
}
\description{
The unconditional (no sample-selection correction) CiC quantile
treatment effect on the treated, at each quantile in \code{quantiles}.
}
\examples{
set.seed(1)
df <- data.frame(
  y = rnorm(200),
  time = rep(1:2, each = 100),
  group = rep(rep(0:1, each = 50), 2)
)
epc <- empirical_cdfs(df, yname = "y", tname = "time", gname = "group",
                      t = 2, tmin1 = 1)
cic_qtt(epc, quantiles = c(0.25, 0.5, 0.75))
}
\seealso{
\code{\link[=cic_bounds]{cic_bounds()}} for the Always-Observed bounds version.
}
